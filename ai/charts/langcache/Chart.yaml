apiVersion: v2
name: langcache
description: >-
  Canonical on-prem Helm chart for LangCache: Control Plane, Data Plane, and
  (bundled or external) Identity Service.
type: application
version: 0.0.1
dependencies:
  - name: redis-iris-onprem-lib
    version: 0.1.0
    repository: https://helm.redis.io/ai
appVersion: "0.0.1"
